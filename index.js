function test(str) {
    alert(str);
}

function intro() {
    window.scrollTo({
        top: 180,
        behavior: 'smooth'
    })
}

function server() {
    window.scrollTo({
        top: 400,
        behavior: 'smooth'
    })
}